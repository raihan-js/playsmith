"""Optional web UI for the Unreal-first flow (install with `pip install -e ".[web]"`).

The core CLI never depends on this package; it's a thin FastAPI front end over the same
UnrealAdapter + director used by `playsmith unreal new`.
"""
