# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/

# Playsmith
# NOTE: .gitignore does NOT support inline comments — keep patterns on their own lines.
# local config (copy from the example):
config/playsmith.yaml
# UI-managed settings + secrets (provider/model/API keys) written by the web Settings panel:
config/playsmith.runtime.yaml
playsmith.runtime.yaml
# generated games live in workspace_dir, never the repo:
playsmith-games/
/workspace/

# OS / editor
.DS_Store
.idea/
.vscode/

# Claude local
.claude/settings.local.json

# Docker secrets
.env
docs/design

# Local planning (ultraplan output — kept locally, not tracked in the repo)
plans/